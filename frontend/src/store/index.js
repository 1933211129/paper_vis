import { createStore } from 'vuex'

export default createStore({
  state: {
    // 纯前端应用，移除后端状态管理
  },
  mutations: {
    // 纯前端应用，移除后端相关mutations
  },
  actions: {
    // 纯前端应用，移除后端相关actions
  },
  modules: {
  }
})
